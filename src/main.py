"""SQLWhisper - Main entry point.

This is the primary entry point for the SQLWhisper application.
Currently serves as a placeholder / development entry point.
"""


def main() -> None:
    """Run the main application.

    This function serves as the primary entry point when the script
    is executed directly. In development it prints a greeting;
    in production it will typically launch the Streamlit UI.
    """
    print("Hello from sqlwhisper!")


if __name__ == "__main__":
    main()
